
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/data
    username: root
    password: 123456

# 设置mybatis-plus相关表配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
